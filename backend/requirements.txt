fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0
celery[redis]==5.4.0
redis==5.2.1
instagrapi==3.0.2
# Account-free downloads of PUBLIC pages: single-post extraction + thumbnails
# + captions via yt-dlp (verified 2026-09-25: mp4 + jpg + info.json, no login).
# Pinned: IG page formats drift, so upgrades are deliberate, not silent.
yt-dlp==2026.8.19
pydantic>=2.9
pydantic-settings==2.6.1
pyjwt==2.10.1
bcrypt==4.2.1
cryptography==43.0.3
slowapi==0.1.9
python-multipart==0.0.19
aiofiles==24.1.0
# NOTE: SOCKS proxies intentionally use the TCP check only (no httpx[socks]
# extra) — the end-to-end HTTPS check runs for http(s) proxies.
httpx==0.28.1
# Sync driver for Celery workers when SYNC_DATABASE_URL is postgresql+psycopg2.
psycopg2-binary==2.9.9
Pillow>=8.1.1
